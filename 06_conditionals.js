let a = prompt("hey what's your age?") //it will ask a question
// a is a string
a = Number.parseInt(a) // Converting the string to number
if (a > 0) {
  alert("this is a valid age")
} else {
  alert("this is not a valid age")
}
